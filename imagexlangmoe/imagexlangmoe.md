# **ImagexLangMOE**

*→ For examples’ purposes we take latent space to be 1D*  
*→ The code works here, no major changes needed*  
*→ 600+ citations*  
*→ However, not really regressive*

| Symbols & Meanings |  |
| ----- | :---- |
| **Prior** | ![][image1] A gaussian distribution of mean \= 0, std \= 1\.  Does not change ever during training.   |
| **Posterior** | Position in latent space, of the point that created the output  |
| **xm** | x is data. m is the modality of data x2 \= Data from 2nd modality *(Pic/text/etc)* **x1:M** \= Data from all modalities |
| **z** | Latent representation. It is a **single point** sampled from the cloud |
| **Variational Posterior, q(z|x)** | The *cloud in latent space* generated by **encoder for mode x**  Read as “The cloud generated, after x is fed in” |
| **q(5|x)** | Height of probability density cloud at z=5  |
| **True joint posterior, p(z|x)** | The ideal/ground truth cloud in latent space. *That is impossible to achieve* |

**Important Equations:**

![][image2]

| Φ | All encoder parameters. EG. Image encoder weights \++ Text encoder weights |
| :---- | :---- |
| **am** | **Equal weight** assigned to all modalities. Can be ½ for 2 modes. |
| **m** | Add together for all modes m  |
| **qφm**  | Latent cloud by encoder for modality m **Φm \=** Parameters of encoder for mode M. |
| **Overall meaning** | *Overall latent cloud \= Sampling of individual latent clouds*   *Explanation of what “Overall” means:* Lets take **2** modalities for example, image\&text. **am** \= ½  ½.qΦ​(z∣x1) \+ ​½.qΦ​(z∣x2) The overall latent cloud qΦ takes ideas from both, but keeps them **separate**  When choosing a point z, **½ chance** of choosing from cloud **qx1**, **½ chance** choosing from cloud **qx2 NOT** the same as choose from qx1, choose from qx2, ½  both and then add them up.  ***EG.** 1D latent space, qx1 has mean= 5, qx2 mean= 15\. Resultant cloud has **2 clouds**: A cloud at 5 & cloud at 15 There is **no “Resultant cloud” at mean \= 10** Each time, z \= \~5 or \~15, will NEVER BE \~10.*   |

* M \= Number of modalities present  
* AMongst modalities present, all are given the same weight of **1/M**  
  * *This prevents one modality from dominating*  
  * *This allows for missing modalities in input.*   
* Better than PoE as a single *overconfident* expert in PoE may overshadow all other experts.  
  * In PoE, all clouds are multiplied. Instead of sampling equally from different clouds.  
    

**ELBO Loss function:**  
![][image3]

*\*This is the exact **same** as Reconstruction-KL loss\!*  
*\*Log form is needed if we sample more than one z\!*  
TLDR:   
**Numerator** \= Recon loss → Derived from Point picked by reparam trick, form latent cloud

**Denominator** \= KL Divergence → Derived from Probability density function of latent cloud. 

| L ELBO | Loss function, of a single input with M modalities *You may do an overall L with batch\_size, idk uty to choose…* Maximised during training  |
| :---- | :---- |
| **Ez∼qΦ(z|x1:M)**  | E \= Expected/Avg value \~ \= Sth sampled from sth **z\~qΦ(z|x1:M)** \= Pick a point **z** from **qΦ(z|x1:M)** → Sample many z from **qΦ(z|x1:M).** Then, take their **average**. → The **qΦ(z|x1:M)** is from the fraction.  *What qΦ(z|x1:M) is has been covered in an earlier section* |
| **log\[pΘ(z,x1:M)\]**  | Defined earlier. **P** \= Probability distribution **Θ** \= All network weights (z,x1:M) \= Joint probability of z & x. ![][image4] ![][image5] Refer to prior definition of prior. Only a part of Numerator \= Recon Loss **p(x|z)**.  **p(z)** multiplies **height of prior** at chosen point in latent space → Further out from 0 \= Numerator Smaller → ⬇️ \= ❌→ **Pushses all points towards the center**   |
| **q(5|x1:M)**  | **q(5|x1:M)**\= Height of **probability density cloud** @ z=5 Natural for density @ expected z to be higher, but it can optimised to be “less high”  *Probability Density/Height  Probability  Probability is heightarea as p=Probability density/height curve Height can exceed 1 greatly if it’s very “Narrow”*  We want L ELBO to be **larger** → Smaller q(5|x) \= ✅✅✅✅ If clouds are more **diffuse** → Probability density @ all points in latent space ⬇️⬇️⬇️ → Density@z  ⬇️⬇️⬇️ → L ELBO ⬆️⬆️  ![][image6]  ![][image7] More diffuse clouds mean better **blending**/**transition in latent space, instead of small clusters** forming at certain points w/ **voids** in between *(Dense & small clouds)*  Is another form of **KL loss.** KL loss seeks to minimise mean, and create a reasonably-sized . *(Balancing between 2&-ln(2))*. The cloud isnt too small, isnt too big. Both achieve better **blending**. ![][image8]  |
| **Overall meaning** | Sample z from q(z|x1:M). Lets say its 5\. Compute **lg\[p(5,x)/q(5|x)\]**. Average over infinitely many samples.  |

**IWAE Loss**

![][image9]

**Upgrade**: Each pass, K samples are taken from the blended cloud (Of all modalities), instead of 1\. K samples are fed individually into decoder. Losses are averaged.

**MMVAE Objective (Looser)**

![][image10]

**Upgrade**: 

* Instead of sampling from the expected of all latent clouds, **we don't mix the clouds**.  
* Instead we sample K times from **each modality's cloud**, and give them **1/M** weightage in the loss.  
* Ensures no modalities can *overpower* others. *“Weighing of gradients of samples from different modalities equally”*

# **Training Flow:**

*We take MNIST \+ SVHN as an example:*  
*(Another example here is CUB image-captions, where its text \+ image)*  
![][image11]

1. Data across modalities are bundled together during DataLoader  
   1.  dataT\[0\] \= MNIST, dataT\[1\] \= SVHN  
2. In [main.py](http://main.py), we run training loop  
   1. Through *argparser*, we set *MNIST as **anchor modality**. This means only MNIST encoder is run.*  
3. *Vae\_mnist.py & vae.py* only looks at MNIST image, **not SVHN image**. A latent cloud exclusively produced by MNIST modality is generated.  
4. **K** points in latent space cloud are picked.  
   1. *qz\_x is the cloud*  
   2. *`rsample` grabs K points in cloud*  
   3. zs \= qz\_x.rsample(torch.Size(\[K\]))

5. All K Latent space points are fed into **Decoders of both MNIST & SVHN**. 2K images are outputted.  
   1. *We have **K** values for denominator, as there **K** points in latent space chosen, giving us **K** values for probability density function.*  
      1. *We take denominator to be **KL divergence,** numerator to be **Recon loss***  
   2. ***‘Recon loss’** for each pair of 2 images is generated → ‘Recon loss’ of each pair is added together → **K** values for **numerator***  
   3. *This gives us **K** different values for fraction below*

   ![][image12]

6. We calculate the **average** *(1/K)* for the K values of the **MNIST anchor**. We then apply **log** on this value.  
   1. *Repeat steps 2a-5 for **SVHN/Other modalities as anchor.***   
   2. *Applying log before step 7, instead of before, is called the ‘**looser**’ lower bound, which is proven to be better in this paper*  
        
7. Now we have 2 *logged* average values. They are **averaged**, since each modality has a weightage of **1/M.** We obtain a **single loss value.**   
   1. *Calculating **1/K average** for each modality separately is what allows us to implement MMVAE loss, where latent clouds **arent mixed**, and have **equal weightage of 1/M**.*   
        
8. This loss is used to update Encoder & Decoder of both MNIST & SVHN by **backprop**  
   1. Loss type is chosen in [objectives.py](http://objectives.py). `def iwae` used here.   
        
9. During *inference* time, not all modalities have to be present → **Min 1** needed for a latent space representation

**MNIST**: Didn't use CNN, only used FC  
**CUB**: Input is not pure image. Its pre-extracted CNN features, from a pretrained CNN such as ResNet. Decoder output is the same 2048\.

**CUB-language:** Each word become 256d. If 10 words, total 2560d. Subsequent CNN layers treat them as 1D image.  

